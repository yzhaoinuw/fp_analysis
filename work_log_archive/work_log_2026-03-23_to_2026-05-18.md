# Work Log Archive: 2026-03-23 to 2026-05-18

Historical project context migrated from `PROJECT_MEMORY.md`, completed planning notes, release history, and Git history during treaty adoption. Entries are newest first.

## 2026-05-18

### Prepare v0.5.0-beta selective spreadsheet workflow (historical record)

- Separated analysis generation from workbook writing in the active desktop app.
- Added selectable spreadsheet types, remembered choices, reset behavior for new MAT files, and background spreadsheet saving with disabled controls.
- Set the application version and release tag to `v0.5.0-beta`.
- Verification:
  - Migration review confirmed the implementation commits, release tag, current code paths, and focused unittest coverage.

## 2026-04-29

### Normalize FP metadata and support NE-only MAT files (historical record)

- Added the `mat_utils.py` signal/frequency boundary so visualization and desktop validation share the same metadata rules.
- Added fallback support for MAT files containing `ne` and `ne_frequency` without `fp_signal_names`.
- Normalized runtime cache keys so downstream callbacks continue to use `fp_signal_names` and `fp_frequency`.
- Verification:
  - Migration review confirmed helper coverage in `tests/test_perievent_analysis.py` and active use from `app_dev.py`.

## 2026-04-27

### Release grouped spreadsheet exports and sleep-bout imports (historical record)

- Released grouped perievent workbooks by signal selection and analysis windows, with primary and fallback save locations.
- Added setup-specific `data_description.txt` tracking, multi-subject append behavior, and same-subject overwrite behavior.
- Added sleep-bout spreadsheet conversion for `0-3` and `1-4` sleep-state codes.
- Published the `v0.4.0` and `v0.4.1` release notes in `CHANGELOG.md`.
- Verification:
  - Migration review confirmed release tags, changelog entries, export helpers, import helpers, and focused tests.

## 2026-03-28

### Make mean exports Prism-friendly (historical record)

- Changed mean perievent and mean cross-correlation sheets to write `<subject>_mean`, `<subject>_sd`, and `<subject>_n` triplets.
- Kept occurrence-level outputs on subject columns keyed by `event_index`.
- Verification:
  - Migration review confirmed current workbook builders and regression tests for the triplet layout.

## 2026-03-23

### Establish the perievent workbook architecture (historical record)

- Refactored the desktop export path around one workbook per signal/analysis type and one sheet per event.
- Used binned downsampling for mean traces and compatibility keys for multi-subject workbook updates.
- Established `event_analysis.py` as the calculation/sheet layer and later extracted selective save orchestration into `analysis_export.py`.
- Verification:
  - Migration review compared the completed export plan with the current implementation and Git history.
